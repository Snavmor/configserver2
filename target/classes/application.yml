server:
   port: 8888
  
spring:
   cloud:
      config:
         server:
            git:
               uri: https://github.com/Snavmor/configserver.git
               default-label: main
               search-paths:
               - '*service'
